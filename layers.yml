layer:
  dev:
    path: packages
    name: demo_${self:custom.stage}_packages
    description: demo ${self:custom.stage} serverless packages
    compatibleRuntimes:
      - python3.12
    allowedAccounts:
      - "*" #for demonstration purposes only
